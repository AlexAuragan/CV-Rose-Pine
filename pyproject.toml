[project]
name = "resume"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "textual>=8.2.8",
    "textual-dev>=1.8.0",
]

[dependency-groups]
dev = [
    "basedpyright>=1.40.0",
    "ruff>=0.16.6",
]

[tool.ruff.lint]
ignore = ["RUF012"]
